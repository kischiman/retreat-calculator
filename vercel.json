{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "env": {
    "VITE_UPSTASH_REDIS_REST_URL": "$KV_REST_API_URL",
    "VITE_UPSTASH_REDIS_REST_TOKEN": "$KV_REST_API_TOKEN"
  }
}